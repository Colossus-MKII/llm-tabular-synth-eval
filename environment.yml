name: tabular-llm-synth
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy
  - pandas
  - openpyxl
  - pip:
      - -e .
      - transformers
      - accelerate
      - datasets
      - safetensors
      - sentencepiece
      - protobuf
      - wandb
      - openai
      - peft
